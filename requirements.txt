# Core FastAPI dependencies
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3

# LangChain and LangGraph - Compatible versions without Rust deps
langgraph==0.0.28
langchain-core==0.1.23
langchain-community==0.0.20
langchain-groq==0.0.1

# LLM Provider
groq==0.4.2

# Search Tools - Using version that doesn't require Rust compilation
# duckduckgo-search 3.9.x is the last stable version before Rust deps
duckduckgo-search==3.9.6

# Environment Variables
python-dotenv==1.0.0

# Additional utilities
typing-extensions==4.9.0
annotated-types==0.6.0

# Explicitly prevent problematic dependencies
# Force lxml to use pre-built wheels (no Rust compilation)
lxml==4.9.3
